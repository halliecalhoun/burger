<li>
	{{name}}

	{{!-- <button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}DEVOUR!{{else}}Devour a burger!{{/if}}
	</button> --}}

	<button class="delete-burger" data-id="{{this.id}}">DEVOUR!</button>
</li>
